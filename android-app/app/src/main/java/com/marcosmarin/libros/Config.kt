package com.marcosmarin.libros

/**
 * The Google Sheet ID is injected from local.properties via BuildConfig.
 *
 * Create or edit `android-app/local.properties` and add:
 *
 *   googleSheetId=<YOUR_SHEET_ID>
 *
 * Then rebuild the app.
 */

const val SHEET_LIBROS = "libros"
const val SHEET_COLORES = "colores"
